const models = require('../../db/models/index');
const helpers = require('../helpers');
const moment = require('moment');

this.createNotification = (obj, userUid, messageId = helpers.generateUid()) => {

}